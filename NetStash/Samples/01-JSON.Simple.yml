input {
tcp {
    host => "listener.logz.io"
    port => 5050
    codec => json_lines
}
}
filter {
  mutate { gsub => ["message", "@($NL$)@", "\r\n"] ,
  add_field => { "token" => "QfPKlorEAulNFrYLSpZtMNnlTPQItEvC" } 
  }
  json { source => message }
}
output {
  elasticsearch {

  }
}
